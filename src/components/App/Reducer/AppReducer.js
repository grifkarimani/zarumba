const initialState = {};

const app = (state = initialState, action) => {
    // switch (action.type) {
    //     case "SET_PAGE":
    //         return state;
    //     default:
    //         return state;
    // }
    return state;
};
export default app;
